name: Deploy Hugo to GitHub Pages

on:
  push:
    branches: [hugo-source]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Hugo
        run: |
          wget -O hugo.deb https://github.com/gohugoio/hugo/releases/download/v0.159.1/hugo_extended_0.159.1_linux-amd64.deb
          sudo dpkg -i hugo.deb

      - name: Build
        run: |
          python3 tools/check_learning_map.py
          hugo --minify

      - name: Build next preview
        run: |
          pip install pyyaml || true
          python3 tools/build_vortex_links.py || echo "links build failed"
          hugo --minify --config hugo.next.toml || echo "next preview build failed"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
