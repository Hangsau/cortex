# 實作清單：Cortex 首頁改成任務入口＋四領域索引

> 來源：對話核准的 `$plan-check`（2026-08-26）
> 工作目錄：`C:\claudehome\projects\my-site`
> 目標：首頁首屏先回答「Cortex 是什麼、我現在能做什麼」，完整四領域與既有目的地仍在兩次操作內可達。
> 執行邊界：不恢復書架、不新增搜尋／新領域頁／圖片／前端框架，不修改各知識 section 內容。原核准範圍不含 commit／push／部署；2026-08-26 站主確認公開網站必須同步後，部署正式納入本清單。

## 設計方向

- 物件／文化參照：當代訓練研究室的工作台，結合研究索引牆與教練夾板；不是期刊目次，也不是 SaaS landing page。
- 材質：深綠布面＋暖色索引紙。
- 字型：沿用站內 Vortex 的 `Cormorant Garamond`、`Noto Serif TC`、`Noto Sans TC`；CJK body 17px、line-height 1.75。
- 色彩：forest `#173C35`、paper `#F4F0E7`、ink `#17211D`、rust `#9B3F26`、rule `#C9C0B1`。
- 密度：首屏同步任務入口最多 6 個；完整目錄漸進揭露。
- 網格：桌面首頁上限 1120px、12 欄、Hero 7/5；領域 2×2；手機單欄。方角、無陰影、無 hover 位移。

## 回歸基準

首頁現有 16 個目的地必須全部保留：

1. `library/essentials-of-strength-training/`
2. `library/essentials-of-strength-training/concepts/`
3. `vortex/`
4. `vortex/drills/`
5. `vortex/instructional/`
6. `vortex/technica/`
7. `vortex/levels/`
8. `vortex/injuries/`
9. `vortex/adm/`
10. `library/mind-for-numbers/`
11. `library/mind-for-numbers/toolkit/`
12. `library/uncommon-sense-teaching/`
13. `library/uncommon-sense-teaching/handbook/`
14. `library/uncommon-sense-teaching/strategies/`
15. `temperament/`
16. `library/`

## 待執行

- [x] **W1：建立基準與確認乾淨工作樹** `[manual]`
  - 記錄 current branch／status、首頁 16 條 href 與 Hugo／Playwright 可用性。
  - 若發現不相關修改，停止並保留，不覆蓋。

- [x] **W2：重構 `data/home.yaml` 的首頁層級** `[manual]`
  - 加入 Hero 主張與 6 個動詞起手的 `quick_actions`。
  - 四領域保留單一真相源；每個 domain 指定 `primary`，其餘為 secondary。
  - 所有色碼限定六位 hex，所有 URL 維持站內相對路徑。

- [x] **W3：重寫 `layouts/index.html`** `[manual]`
  - Hero 不再重複 nav 的 `Cortex`，改用用途主張作 `h1`。
  - 首屏渲染 6 個常用任務；四領域在第二層以 2×2 atlas 呈現。
  - 次要入口用原生 `<details>` 漸進揭露，無 JavaScript。
  - 保留正確 heading hierarchy、可讀連結文字與 `.Site.BaseURL` 子路徑安全。

- [x] **W4：重寫 `static/css/home.css`** `[manual]`
  - 實作深綠 Hero、暖紙內容區、1120px 空間化首頁與 12 欄網格。
  - 1280／768／390／320px 無水平溢出；點擊目標至少 24×24px。
  - 文字、邊框、focus ring 達 WCAG 2.2 AA；加入 `:focus-visible` 與 `prefers-reduced-motion`。
  - 不改 `variables.css`、`layout.css` 或其他 section CSS。

- [x] **W5：新增 `tools/home_audit.js`** `[manual]`
  - 驗 6 個 quick actions、4 個 domains、16 個既有目的地零遺漏／規格外 URL；捷徑最多只與 canonical 入口重複一次。
  - 驗所有站內連結回應成功、heading 不跳級、文字與非文字對比、focus 可見、點擊尺寸。
  - 驗 desktop／mobile 無水平溢出，mobile 主要內容高度不超過三個 viewport。
  - 驗 `<details>` 在無 JS 下可用，且 secondary route 兩次操作內可達。

- [x] **W6：同步專案文件** `[manual]`
  - 更新 `CLAUDE.md` 首頁規則、`MAP.md` 導航地圖、`HANDOFF.md` 狀態與驗收結果。
  - 不改既有知識內容或其他專案紀錄。

- [x] **W7：全套驗收與人工 code audit** `[manual]`
  - `git diff --check`
  - `hugo --gc --minify`
  - `node tools/home_audit.js`
  - `node tools/audit.js`（既有 38/38 不退化）
  - Playwright 1280×900、390×844、320×800 截圖巡檢；暫存截圖用完刪除。
  - 人工檢查 diff 僅命中本清單列出的檔案、沒有 inline style／`!important`／新 JS。

- [ ] **W8：提交、部署與線上驗證** `[manual]`
  - 提交首頁改版並 push 至 `hugo-source`。
  - 等待 GitHub Pages workflow 成功，不以「已 push」代替「已上線」。
  - 對公開網址重跑首頁 audit，確認 6 個任務入口、16 個目的地與 responsive 規格實際生效。
  - 將 commit／CI／線上驗證結果回寫 `HANDOFF.md` 與本清單。

## 進行中

- W8：部署範圍已由站主補充核准；準備 commit、push、等待 CI 並核對公開首頁。

## 已完成

- W1：`hugo-source` 基準工作樹乾淨；Hugo 0.162.1 production build 130 頁、exit 0。
- W2–W4：首頁資料、Hugo layout、專用 CSS 已完成；無新頁面／JS／圖片。
- W5：`home_audit.js` 28/28；home.yaml 色碼/URL 格式與 16 個目的地全部通過，字級固定 14/17/22/26px，390px 2.76 viewport、320px 2.96。
- W6：`CLAUDE.md` / `MAP.md` / `HANDOFF.md` 已同步新首頁角色、schema 與驗收方式。
- W7：Hugo production build 130 頁、首頁 audit 28/28、既有 audit 38/38、`git diff --check` 乾淨；1280/768/390/320px 截圖已人眼巡檢。
- A–I 過閘：A（6 任務／3 模板／漸進揭露）、B（動詞連結與倒金字塔）、C/D（14/17/22/26px 四級、CJK body 17px/1.75、只對 Latin/數字加字距）、E（AA 對比／24px 目標／320px reflow）、F（主流 nav＋12 欄分群）、G/H（任務優先、領域空間索引）、I（focus／reduced-motion／CLS 0）全部通過。
- `check_map_freshness.py` 已跑；首頁路徑有同步，工具另報既有 helper/Vortex 檔未列 MAP，明記為本次範圍外 drift。
- 本輪本機 Hugo server 已停止；`C:\claudehome\tmp\cortex-home-*` build、截圖與 log 已刪，皆可由 build/audit 重生。

## 風險與回復

- YAML schema／色碼／URL 錯誤：Hugo build + audit 擋下；回復該 W 的局部 patch。
- 次要入口變難找：固定比對 16 個目的地，全部需在兩次操作內可達。
- CJK 長字串／400% zoom 溢出：320px browser audit；失敗則退回單欄與可換行規則。
- web font FOUT／CLS：量測 layout shift；若不穩定則移除首頁外部字型，保留 CJK system fallback。
- partial edit：任何本機驗收失敗不部署；CI 或線上驗證失敗則保留證據並修正，不把半成品標成完成。

## 完成門檻

- [x] Hugo production build exit 0。
- [x] 首頁 audit 全綠，16 個目的地零遺漏、零規格外 URL、零死連結；捷徑重複受控。
- [x] 既有 `tools/audit.js` 38/38。
- [x] 1280／390／320px 人眼確認無破版、無橫向捲動、資訊焦點明確。
- [x] A–I 鐵則逐條過閘；不適用項有理由。
- [x] 文件同步且 `git diff --check` 乾淨。
- [ ] `hugo-source` commit 已 push、GitHub Pages CI 成功，公開首頁 audit 全綠。
