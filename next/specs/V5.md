# V5：運動員發展矩陣 ADM（總覽、矩陣、技術標準）

`vortex/adm/background/` 長文已由 Claude 掛入（`content/vortex/adm/background.md`，用 article 版型），**不用你做**，但總覽頁要連到它。

## 產出檔（只准建立這些）
- `next/content/vortex/adm/_index.md`（`title: 運動員發展矩陣 ADM`、`description:` 一句中文、`params: {role: adm-home}`）
- `next/content/vortex/adm/matrix.md`（`title: 發展矩陣`、`params: {role: adm-matrix}`）
- `next/content/vortex/adm/standards.md`（`title: 技術標準`、`params: {role: adm-standards}`）
- `next/layouts/_partials/vortex/adm-home.html`、`adm-matrix.html`、`adm-standards.html`
- `next/assets/css/adm.css`

## 資料（唯讀）
`data/adm/matrix.yaml`（4 支柱 × 4 階段，含 summary 與 points）、`data/adm/standards.yaml`（22 項技術標準，phases／criteria 結構）。舊版型 `layouts/vortex/vortex-adm-*.html` 可參考欄位與順序；階段中文名：L2T 學習訓練、T2T 訓練為訓練、T2C 訓練為競賽、T2W 訓練為勝利（年齡對照照舊版型）。

## 規格
- **adm-home**（`lib-series`）：`ls-hero`（kicker「Vortex · 長期規劃」、`ls-title`、`ls-lead`）；三個入口 `ls-entries`：發展矩陣（`vortex/adm/matrix/`）、技術標準（`vortex/adm/standards/`）、背景與發展架構（`vortex/adm/background/`），各附一句說明
- **adm-matrix**（`<article class="lib-chapter adm-wide">`）：以表格呈現「支柱 × 階段」（`<table class="adm-table">`，列＝支柱、欄＝四階段，格內 summary ＋ points 清單，每個 `<td>` 帶 `data-label="<階段中文名>"`）；≤900px 由 CSS 改成直排，同一份 HTML 不重複輸出內容
- **adm-standards**（`<article class="lib-series" data-list>`）：泳式篩選列 ＋ 22 項標準清單 `<ol class="items">`；每項 `<li data-stroke="<key>">`：名稱、phases 各階段 criteria；篩選沿用 `next/assets/js/filter.js`（W7 已建，先讀該檔確認它要的 DOM 介面再用，不准修改它）
- 文字 markdownify

## adm.css
- `.adm-wide { max-width: 1040px; }`；`.adm-table`：寬 100%、`--fs-s`、表頭上 2px `--ink`、格線 1px `--rule`、格內 `padding: var(--s1)`、垂直置頂
- ≤900px：`.adm-table, .adm-table tbody, .adm-table tr, .adm-table td { display: block; }`，隱藏表頭列，每格前用 `td::before { content: attr(data-label); }` 顯示階段名

## 驗收
```
python -X utf8 next/specs/check.py V5
```
