# V2：「什麼是水感」導讀頁

舊站 `layouts/vortex/vortex-water-sense.html` 把整篇導讀**寫死在版型裡**（對應的 content 檔是空的）。本單把這篇的文字搬成 Markdown 內容檔，由新版長文版型（`vortex/article.html`）呈現。

## 產出檔（只准建立這一個）
- `next/content/vortex/water-sense.md`

## 規格
- front matter：`title: 什麼是水感`、`description:`（一句中文，取原頁導言的第一句）、`layer_name: 入門`、`weight: 1`
- 內文：把舊版型裡**所有讀者看得到的文字**依原順序轉成 Markdown：大標轉 `##`、小標轉 `###`、段落、清單、表格照原樣。**逐字保留**，不改寫、不摘要、不增刪。
- 每個 `##` 標題加穩定錨點 `{#英文-slug}`（依內容自訂，小寫英文與連字號）。
- 舊版型裡的導航、按鈕這類介面文字不要搬。指向站內其他 Vortex 頁的連結寫成相對路徑 Markdown 連結（本頁網址是 `vortex/water-sense/`，所以自由式頁寫 `../free/`、級別清單寫 `../levels/`）。
- 驗收會比對：舊版型中所有長度 ≥ 12 字的中文句子，有 95% 以上出現在新頁面。

## 驗收
```
python -X utf8 next/specs/check.py V2
```
