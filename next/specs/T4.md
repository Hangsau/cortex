# T4：肌動學學習地圖

## 產出檔（只准建立這些）
- `next/content/library/kinesiology/learning-map.md`（front matter 取舊檔 `content/library/kinesiology/learning-map.md` 的 title、description，另加 `params: {role: reader-map}`；內文照抄舊檔內文）
- `next/layouts/_partials/library/reader-map.html`
- `next/assets/css/learning-map.css`
- 若需要互動：`next/assets/js/learning-map.js`

## 資料與舊檔（唯讀）
`data/reading/learning-map.json`；舊版型（在 `layouts/library/` 找 layout 名 `reading-map` 的檔）與其用到的 JS。`tools/check_learning_map.py` 可以看出這份地圖的資料契約。

## 規格
- 版面（`lib-series`）：`ls-hero` → 地圖內容依資料結構呈現（問題 → 對應章節與小節，連到新站 `library/kinesiology/chNN/#錨點`），保留舊版的閱讀順序與分組
- 所有連到章節的 URL 必須存在於新站

## 驗收
```
python -X utf8 next/specs/check.py T4
```
