# T3：氣質自測（家長版／成人版）

## 產出檔（只准建立這些）
- `next/content/temperament/quiz.md`（`title: 氣質自測`、`description:` 一句中文、`params: {role: temp-quiz}`）
- `next/layouts/_partials/library/temp-quiz.html`
- `next/assets/js/temp-quiz.js`
- `next/assets/css/temp-quiz.css`

## 資料與舊檔（唯讀）
`data/temperament/quiz.yaml`（scoring、prototypes、items_child、items_self、advice、type_text、type_label）、`data/temperament/traits.yaml`。舊版 `layouts/temperament/temperament-main.html`（測驗部分）與 `static/js/temperament-quiz.js`（計分邏輯）。**計分邏輯必須與舊版完全相同**。

## 規格
- 題庫與計分參數以 `<script type="application/json" id="tq-data">{{ ... | jsonify }}</script>` 嵌入頁面
- 版面（`lib-chapter`）：`lc-head`（kicker「兒童九種氣質 · 工具」）→ 說明（**非診斷**；結果是九維度剖面＋三型傾向，不是好壞分數）→ 選模式（幫孩子測／測自己）→ 題目（每題選項按鈕 `min-height: var(--tap)`）→ 結果：九維度剖面（每維度一列：名稱連到 `temperament/traits/<key>/`、位置用文字＋一條 CSS 長條表示，不只靠顏色）、三型傾向文字、環境配合建議（advice）
- 不送出任何資料；可選擇存 localStorage
- 無 JS 時 `<noscript>` 說明

## 驗收
```
python -X utf8 next/specs/check.py T3
```
